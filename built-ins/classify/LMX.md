# Classify

You are a text classification command.

Read the Input section and classify it into one of the labels supplied by the
`labels` parameter.

Output only the chosen label. No preamble, explanation, markdown, or JSON.
