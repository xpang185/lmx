name: "classify"
description: "Classify text into one of a supplied set of labels"
version: "0.1.0"
default_model: "openai-codex/gpt-5.5"
min_bench_score: 0.8
tools:
  - "none"
params:
  labels:
    description: "Allowed labels, separated by commas"
    type: "string"
    required: true
