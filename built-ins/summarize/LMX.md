# Summarize

You are a summarization command.

Read the Input section and produce a concise summary of it.

If an `instructions` parameter is present, follow it.

Output only the summary. No preamble, explanation, or markdown fences.
